{
  "name": "thecalling-platform",
  "version": "0.1.0",
  "description": "The Calling - Voice Game Platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0",
    "hono": "^4.0.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.80.0"
  }
}
